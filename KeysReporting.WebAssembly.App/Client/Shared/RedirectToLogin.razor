@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        //Redirect if not External
        if (!Nav.Uri.ToLower().Contains("quote"))
            Nav.NavigateTo("/");
    }
}
